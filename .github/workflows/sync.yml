name: YouTube Music Scrobble Sync

on:
  schedule:
    - cron: '0 23 * * *'  # Daily at 23:00 UTC (adjust as needed)
  push:
    branches: [master] # Trigger on push to main branch
  workflow_dispatch: # Allows manual triggering from GitHub UI

jobs:
  scrobble:
    runs-on: ubuntu-latest
    environment: production # Binds to the 'production' environment for secrets and protection rules
    outputs:
      scrobble_log: ${{ steps.scrobbler.outputs.scrobble_log }}
      notification_data: ${{ steps.scrobbler.outputs.notification_data }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11' # Or your preferred Python version (e.g., '3.8', '3.9', '3.10')
          
      - name: Cache dependencies
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-
      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Cache scrobble database
        uses: actions/cache@v3
        with:
          path: data.db
          key: ${{ runner.os }}-scrobble-db

      - name: Run YouTube Music Scrobbler
        id: scrobbler
        env:
          LAST_FM_API: ${{ secrets.LAST_FM_API }}
          LAST_FM_API_SECRET: ${{ secrets.LAST_FM_API_SECRET }}
          YTMUSIC_COOKIE: ${{ secrets.YTMUSIC_COOKIE }}
          LASTFM_SESSION: ${{ secrets.LASTFM_SESSION }}
        run: |
          echo "Starting YouTube Music Scrobbler..."
          output=$(python start_standalone.py)
          echo "$output"
          
          log_output=$(echo "$output" | sed 's/--- NOTIFICATION_DATA_START ---/--- NOTIFICATION_DATA_END ---/g' | sed -n '1,/--- NOTIFICATION_DATA_END ---/p' | sed 's/--- NOTIFICATION_DATA_END ---//g')
          json_output=$(echo "$output" | awk '/--- NOTIFICATION_DATA_START ---/{f=1;next} /--- NOTIFICATION_DATA_END ---/{f=0} f')

          echo "scrobble_log<<EOF" >> $GITHUB_OUTPUT
          echo "$log_output" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

          echo "notification_data<<EOF" >> $GITHUB_OUTPUT
          echo "$json_output" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

          echo "YouTube Music Scrobbler finished."
        
  notify:
    needs: scrobble
    runs-on: ubuntu-latest
    if: failure()
    environment: production
    steps:
      - name: Send Discord notification
        run: |
          curl -H "Content-Type: application/json" -d '{
            "content": "YouTube Music Scrobble Sync failed!"
          }' "$DISCORD_WEBHOOK_URL"
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}

  notify_success:
    needs: scrobble
    runs-on: ubuntu-latest
    if: success()
    environment: production
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Send Discord notification
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          NOTIFICATION_DATA: ${{ needs.scrobble.outputs.notification_data }}
        run: |
          pip install requests
          python .github/scripts/send_success_notification.py